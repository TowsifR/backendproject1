apiVersion: v1
kind: Service
metadata:
  labels:
    app: grafana-svc
  name: grafana-svc
spec:
  selector:
    app: prometheus-grafana
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
  type: ClusterIP